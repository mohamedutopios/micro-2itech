server:
  port: 8888

spring: 
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mohamedutopios/server-config
          clone-on-start: true
          default-label: master
